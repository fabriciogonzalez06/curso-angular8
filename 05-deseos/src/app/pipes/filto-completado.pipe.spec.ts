import { FiltoCompletadoPipe } from './filto-completado.pipe';

describe('FiltoCompletadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltoCompletadoPipe();
    expect(pipe).toBeTruthy();
  });
});
